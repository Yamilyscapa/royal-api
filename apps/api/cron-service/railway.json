{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bun install && bun run build"
  },
  "deploy": {
    "startCommand": "bun dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "cron": {
      "schedule": "*/5 * * * *"
    }
  }
}
